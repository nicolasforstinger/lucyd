# Lucyd — production dependencies
# Both provider SDKs are optional. Install only what your providers.d/ config needs.

tiktoken>=0.7,<1     # Token counting (cl100k_base). Falls back to byte estimate if absent.
anthropic>=0.45      # Required if using provider = "anthropic-compat"
openai>=2.0,<3       # Required if using provider = "openai-compat" or memory embeddings
httpx>=0.27,<1       # Required for Whisper voice transcription (transitive dep of anthropic, but explicit for standalone use)
aiohttp>=3.9,<4      # HTTP API server

# Optional — install if using these features
Pillow~=12.0         # Required if [vision] enabled (image processing)
pypdf~=6.7.5         # Required if [documents] enabled (PDF text extraction). >=6.7.5 for CVE-2026-28804 fix.
