# Lucyd — install what you use
# Both are optional. Install only the SDK(s) for your configured provider(s).

anthropic>=0.45.0    # Required if using provider = "anthropic-compat"
openai>=2.0.0        # Required if using provider = "openai-compat" or memory embeddings
httpx>=0.27.0        # Required for Whisper voice transcription (transitive dep of anthropic, but explicit for standalone use)
aiohttp>=3.9.0       # Required if [http] enabled = true (HTTP API server)

# Optional — install if using these features
Pillow>=12.0         # Required if [vision] enabled (image processing)
pypdf>=6.7.5         # Required if [documents] enabled (PDF text extraction). >=6.7.5 for CVE-2026-28804 fix.

# Test dependencies
pytest>=8.0
pytest-asyncio>=0.23

# Dev dependencies
pip-audit>=2.7.0
mutmut>=3.4.0
ruff>=0.15.0
