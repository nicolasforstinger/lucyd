# Lucyd — install what you use
# Both are optional. Install only the SDK(s) for your configured provider(s).

anthropic>=0.45.0    # Required if using provider = "anthropic-compat"
openai>=1.60.0       # Required if using provider = "openai-compat" or memory embeddings
httpx>=0.27.0        # Required for Whisper voice transcription (transitive dep of anthropic, but explicit for standalone use)
aiohttp>=3.9.0       # Required if [http] enabled = true (HTTP API server)

# Test dependencies
pytest>=8.0
pytest-asyncio>=0.23

# Dev dependencies
pip-audit>=2.7.0
